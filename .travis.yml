dist: xenial
language: csharp
solution: Banshee.sln

addons:
  apt:
    packages:
      - autopoint
      - gtk-sharp3
      - libdbus-glib2.0-cil
      - libdbus-glib2.0-cil-dev
      - libdbus2.0-cil
      - libgconf2-dev
      - libgconf2.0-cil
      - libgconf2.0-cil-dev
      - libglib2.0-dev
      - libglib3.0-cil-dev
      - libgstreamer-plugins-base1.0-dev
      - libgstreamer1.0-dev
      - libgtk-3-dev
      - libgtk3.0-cil
      - libgtk3.0-cil-dev
      - libgudev3.0-cil
      - libmono-addins-cil-dev
      - libmono-addins0.2-cil
      - libmono-upnp-cil
      - libmono-zeroconf1.0-cil
      - libmtp-dev
      - libsqlite3-dev
      - libtaglib-cil-dev
      - libtaglib2.1-cil
      - libtool
      - mono-devel

script:
  - autoreconf -fvis && ./configure && make
